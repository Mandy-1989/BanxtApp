import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    viewParent: {
        flex: 1,
        alignItems: 'center',
        justifyContent:'center'
    }
})

export { styles }